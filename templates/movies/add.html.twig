{% extends "./base.html.twig" %}

{% block title %}
    Add Movies
{% endblock %}
{% block body %}
    <div class="container">
        {{ form_start(form) }}

        {{ form_widget(form) }}
        <button type="submit" class="submitBtn btn btn-primary ">Submit</button>
        {{ form_end(form) }}
    </div>

{% endblock %}